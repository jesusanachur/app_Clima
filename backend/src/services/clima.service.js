import axios from 'axios';
import { createLogger } from '../utils/logger.js'; // Asegurar que esta ruta es correcta

const logger = createLogger('clima:service');

// ... el resto del código se mantiene igual